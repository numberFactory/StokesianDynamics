[build-system]
requires = [
    "scikit-build-core>=0.5.0",
    "nanobind>=2.0",
]
build-backend = "scikit_build_core.build"

[project]
name = "Lubrication"
version = "0.1.0"
description = "Lubrication corrections for rigid body hydrodynamics via nanobind"
requires-python = ">=3.8"